# =============================================================
# secrets/sqlserver-secret.yaml
# Credenciales sensibles. NUNCA subir con valores reales al repo.
# Usar variables o kubectl create secret en su lugar.
# Aplicar: kubectl apply -f kubernetes/secrets/
# =============================================================
apiVersion: v1
kind: Secret
metadata:
  name: sqlserver-secret
  namespace: inventario
type: Opaque
stringData:
  SECRET_KEY: "cambiar-esta-clave-por-una-larga-y-aleatoria"
  SQL_USER: "sa"
  SQL_PASSWORD: "TuPasswordFuerte123"
  MONGO_USER: ""
  MONGO_PASSWORD: ""
